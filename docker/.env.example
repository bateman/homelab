# =============================================================================
# Homelab Docker Stack - Environment Variables
# =============================================================================
# This file contains ONLY non-sensitive configuration.
# Credentials are in .env.secrets (see .env.secrets.example)
#
# Setup:
#   1. cp .env.example .env
#   2. cp .env.secrets.example .env.secrets
#   3. Customize values in both files
#
# IMPORTANT: Never commit .env and .env.secrets to Git!
# =============================================================================

# -----------------------------------------------------------------------------
# User/Group IDs
# -----------------------------------------------------------------------------
# MUST match the dockeruser owner of /share/data on NAS
# After creating dockeruser on QNAP, get values with: id dockeruser
# Example output: uid=1001(dockeruser) gid=100(everyone) groups=100(everyone)
#                      ^^^^          ^^^
#                      PUID          PGID
PUID=1001
PGID=100

# -----------------------------------------------------------------------------
# Timezone
# -----------------------------------------------------------------------------
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=Europe/Rome

# -----------------------------------------------------------------------------
# Network Configuration
# -----------------------------------------------------------------------------
# NAS IP address (VLAN 3 - Servers)
NAS_IP=192.168.3.10

# qBittorrent incoming connections port
# Must match: qBittorrent Settings -> Connection -> Listening Port
# Must be open on router if using torrents
QBIT_PORT=50413

# -----------------------------------------------------------------------------
# Tailscale Configuration
# -----------------------------------------------------------------------------
# Subnet routes to advertise (comma-separated CIDR ranges)
# Allows remote Tailscale clients to access these local networks
TS_ROUTES=192.168.3.0/24,192.168.4.0/24

# Extra Tailscale arguments (optional)
# TS_EXTRA_ARGS=--accept-routes

# -----------------------------------------------------------------------------
# VPN Configuration
# -----------------------------------------------------------------------------
# Docker Compose profile for download clients
# - vpn: Route qBittorrent/NZBGet through Gluetun VPN (recommended)
# - novpn: Direct connection without VPN protection
#
# When using vpn profile, you MUST configure VPN credentials in .env.secrets
# See docs/setup/vpn-setup.md for provider-specific setup
COMPOSE_PROFILES=vpn
