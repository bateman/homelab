# =============================================================================
# Homelab Docker Stack - Environment Variables
# =============================================================================
# Copy this file to .env and customize values
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to version control!
# =============================================================================

# -----------------------------------------------------------------------------
# Pi-hole Configuration
# -----------------------------------------------------------------------------
# Password for Pi-hole web interface
#
# ⚠️  SECURITY WARNING: Change this immediately after setup!
# Generate a secure password with: openssl rand -base64 24
#
PIHOLE_PASSWORD=CHANGE_ME_IMMEDIATELY

# -----------------------------------------------------------------------------
# Traefik Dashboard Authentication
# -----------------------------------------------------------------------------
# Basic auth for Traefik dashboard (user:password_hash format)
# Default: admin / admin (CHANGE THIS!)
#
# Generate your own hash:
#   echo $(htpasswd -nB admin) | sed -e s/\\$/\\$\\$/g
#
# Example output: admin:$$2y$$05$$xxxxx... (use this format)
#
# TRAEFIK_DASHBOARD_AUTH=admin:$$2y$$05$$your_hash_here

# -----------------------------------------------------------------------------
# Watchtower API Token
# -----------------------------------------------------------------------------
# Token for Watchtower HTTP API metrics endpoint
# Used for healthcheck; change if exposing externally
WATCHTOWER_API_TOKEN=watchtower

# -----------------------------------------------------------------------------
# Common User/Group IDs
# -----------------------------------------------------------------------------
# These MUST match the dockeruser owner of /share/data on the NAS
# After creating dockeruser on QNAP, get values with: id dockeruser
# Example output: uid=1000(dockeruser) gid=100(everyone) groups=100(everyone)
#                      ^^^^          ^^^
#                      PUID          PGID
PUID=1000
PGID=100

# -----------------------------------------------------------------------------
# Timezone
# -----------------------------------------------------------------------------
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=Europe/Rome

# -----------------------------------------------------------------------------
# Network Configuration
# -----------------------------------------------------------------------------
# NAS IP address (VLAN 3 - Servers)
NAS_IP=192.168.3.10

# qBittorrent incoming connections port
# Must match: qBittorrent Settings -> Connection -> Listening Port
# Must be port-forwarded on router if using torrents
QBIT_PORT=50413

# -----------------------------------------------------------------------------
# Watchtower Notifications (optional)
# -----------------------------------------------------------------------------
# Uncomment and configure for update notifications
# See: https://containrrr.dev/watchtower/notifications/
# WATCHTOWER_NOTIFICATION_URL=
# WATCHTOWER_NOTIFICATION_TEMPLATE=

# -----------------------------------------------------------------------------
# API Keys (retrieved from service settings after first run)
# -----------------------------------------------------------------------------
# These are auto-generated by each service on first start
# Retrieve from: Settings -> General -> API Key
#
# SONARR_API_KEY=
# RADARR_API_KEY=
# LIDARR_API_KEY=
# PROWLARR_API_KEY=
# BAZARR_API_KEY=
