# =============================================================================
# Homelab Docker Stack - Environment Variables
# =============================================================================
# Questo file contiene SOLO configurazione non sensibile.
# Le credenziali sono in .env.secrets (vedi .env.secrets.example)
#
# Setup:
#   1. cp .env.example .env
#   2. cp .env.secrets.example .env.secrets
#   3. Personalizza i valori in entrambi i file
#
# IMPORTANTE: Non committare .env e .env.secrets in Git!
# =============================================================================

# -----------------------------------------------------------------------------
# User/Group IDs
# -----------------------------------------------------------------------------
# DEVONO corrispondere all'utente dockeruser proprietario di /share/data sul NAS
# Dopo aver creato dockeruser su QNAP, ottieni i valori con: id dockeruser
# Output esempio: uid=1000(dockeruser) gid=100(everyone) groups=100(everyone)
#                      ^^^^          ^^^
#                      PUID          PGID
PUID=1000
PGID=100

# -----------------------------------------------------------------------------
# Timezone
# -----------------------------------------------------------------------------
# Vedi: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=Europe/Rome

# -----------------------------------------------------------------------------
# Network Configuration
# -----------------------------------------------------------------------------
# Indirizzo IP del NAS (VLAN 3 - Servers)
NAS_IP=192.168.3.10

# Porta connessioni in entrata qBittorrent
# Deve corrispondere a: qBittorrent Settings -> Connection -> Listening Port
# Deve essere aperta sul router se usi torrent
QBIT_PORT=50413
