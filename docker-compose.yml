# =============================================================================
# DEPRECATED - Questo file e' mantenuto per retrocompatibilita'
# =============================================================================
#
# La configurazione e' stata suddivisa in:
#   - compose.yml       -> Servizi infrastruttura (Pi-hole, HA, Portainer, Watchtower)
#   - compose.media.yml -> Stack media (download clients, *arr, monitoring)
#
# USO RACCOMANDATO:
#   docker compose -f compose.yml -f compose.media.yml up -d
#   oppure semplicemente: make up
#
# Questo file include entrambi i file per retrocompatibilita' con:
#   docker compose up -d
# =============================================================================

include:
  - compose.yml
  - compose.media.yml
