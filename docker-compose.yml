# =============================================================================
# DEPRECATED - This file is kept for backwards compatibility
# =============================================================================
#
# Configuration has been split into:
#   - compose.yml       -> Infrastructure services (Pi-hole, HA, Portainer, Watchtower)
#   - compose.media.yml -> Media stack (download clients, *arr, monitoring)
#
# RECOMMENDED USAGE:
#   docker compose -f compose.yml -f compose.media.yml up -d
#   or simply: make up
#
# This file includes both files for backwards compatibility with:
#   docker compose up -d
# =============================================================================

include:
  - compose.yml
  - compose.media.yml
